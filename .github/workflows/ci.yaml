name: CI
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  opa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: open-policy-agent/setup-opa@v2
      - run: opa test policy
